{
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "hardhat-compile": "npx hardhat compile",
    "staking-test": "hardhat test ./test/staking.js",
    "staking-deploy": " npx hardhat run --network testnet ./scripts/deploy.js",
    "start": "node ./index.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.10.2"
  },
  "dependencies": {
    "@alch/alchemy-web3": "^1.4.6",
    "@openzeppelin/contracts": "^4.7.3",
    "web3": "^1.7.5"
  }
}
